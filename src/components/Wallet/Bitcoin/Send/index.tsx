import { Box, Stack, Typography } from '@mui/material';

const BitcoinSend = () => {
  return (
    <Box>
      <Stack direction={'row'} alignItems={'center'}>
        <Typography>Payment Send</Typography>
      </Stack>
    </Box>
  );
};

export default BitcoinSend;
